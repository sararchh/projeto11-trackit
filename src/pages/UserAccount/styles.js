import styled from "styled-components";

export const Button = styled.button`
  width: 150px;
  height: 80px;
  background-color: var(--blue);
  border-radius:5px;

  color: var(--white);
  font-size: 20px;
`;